package com.revature.q07;

import static org.junit.Assert.*;

import java.util.Arrays;

import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;

public class Tests {

	@BeforeClass
	public static void setUpBeforeClass() throws Exception {
	}

	@AfterClass
	public static void tearDownAfterClass() throws Exception {
	}

	@Before
	public void setUp() throws Exception {
	}

	@After
	public void tearDown() throws Exception {
	}

	@Test
	public void ageComparatorTest() {
		Employee[] sorted = new Employee[3];
		
		e[0] = new Employee("youngin", "crib", 2);
		e[1] = new Employee("sdsd", "2123", 65);
		e[2] = new Employee("turlte", "who dis", 99);
		
		Employee[] tosort = new Employee[3];
		e[0] = new Employee("sdsd", "2123", 65);
		e[1] = new Employee("turlte", "who dis", 99);
		e[2] = new Employee("youngin", "crib", 2);
		Arrays.sort(e, new EmployeeAgeComparator());
		
	}

}
