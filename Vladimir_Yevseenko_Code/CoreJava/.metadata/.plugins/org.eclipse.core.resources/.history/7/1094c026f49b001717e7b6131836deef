package com.revature.servlets;

import java.io.IOException;

import javax.servlet.ServletException;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.apache.log4j.Logger;
import org.json.simple.JSONObject;

import com.revature.hashing.Hasher;
import com.revature.logging.Logging;
import com.revature.pojos.User;
import com.revature.service.Service;

public class ChangeUserInfoServlet extends HttpServlet {
	private static final long serialVersionUID = -2203144656220389003L;

	private static Logger logger = Logging.getLogger();
	
	@SuppressWarnings("unchecked")
	@Override
	protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
		logger.debug("ChangeUserInfoServlet doPost()");
		
		Service service = Service.getFromSession(req.getSession());
		User curUser = service.getCurUser();
		
		String first = req.getParameter("first");
		if (first == null)
			first = curUser.getFirst();
		
		String last = req.getParameter("last");
		if (last == null)
			last = curUser.getLast();
		
		String email = req.getParameter("email");
		if(email == null)
			email = curUser.getEmail();
		
		String password = req.getParameter("password");
		if (password == null)
			password = curUser.getPassword();
		
		Hasher hasher = new Hasher();
		String passwordHash = hasher.hashPassword(password);
		
		JSONObject obj = new JSONObject();
		
		resp.setContentType("application/json");
		
		obj.put("success", service.updateUserInfo(first, last, email, passwordHash));
		
		logger.debug("ChangeUserInfoServlet first: " + first);
		logger.debug("ChangeUserInfoServlet last: " + last);
		logger.debug("ChangeUserInfoServlet email: " + email);
		logger.debug("ChangeUserInfoServlet password: " + password);
		logger.debug("ChangeUserInfoServlet passwordHash: " + passwordHash);
		
		resp.getWriter().println(obj);
	}
}
