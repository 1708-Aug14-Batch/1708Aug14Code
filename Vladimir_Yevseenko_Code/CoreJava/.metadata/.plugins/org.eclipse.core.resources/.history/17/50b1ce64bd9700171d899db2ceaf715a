$(function() {
	$.post('yourReimbursementsServlet',
			JSON.stringify({}),
			function(data) {
				console.log(data);
				for (let i=0; i<data.length; i++) {
					console.log(data[i]);
					//$('#reimbursementList').append('<li>' + reimb.id + '</li>');
				}
			},
			'JSON');
});