/**
 * 
 */

window.onload = loadDashboardView;

function loadDashBoardView() {
	var xhr = new XMLHttpRequest();
	xhr.onreadystatechange = function() {
		if (xh.readyState == 4 && xhr.status == 200) {
			console.log(xhr.responseText);
		}
	};
}
