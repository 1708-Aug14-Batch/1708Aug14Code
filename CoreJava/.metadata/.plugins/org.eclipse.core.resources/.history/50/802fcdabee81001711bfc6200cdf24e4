package com.example.fileio;

public class FileIO {
	
	public static final String FILENAME = "src/com/example/fileio/log.txt";
	
	static void writeMessage(String message) {
		
		try {
			BufferedWriter bw = new BufferedWriter(new FileWriter(FILENAME, false));
			bw.write(message + '\n');
		}
	}

}
