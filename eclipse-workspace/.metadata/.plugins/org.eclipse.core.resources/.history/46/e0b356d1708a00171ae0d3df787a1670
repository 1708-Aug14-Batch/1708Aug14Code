package com.bank.dao;

import java.util.ArrayList;

public interface IDAO<T> {
	
	int create(T entity);
	
	ArrayList<T> readAll();
	
	T read(int id);
	
	int update(T entity);
	
	int disable(int id);

}
