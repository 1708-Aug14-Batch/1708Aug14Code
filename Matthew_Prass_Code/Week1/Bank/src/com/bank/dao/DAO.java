package com.bank.dao;

public interface DAO {

	//the contract we make for the info that we will need to send to and receive from the data source
	
	//addUser
	//editUser - change any user variables(except id)
	//getUser
}
