package com.question15;

public interface MathI { //MathInterface Interface
	/*
	 * Creating the abstract functions of the interface. They are abstract because the default unless specified is abstract.
	 */
	int addNum(int a, int b);
	int subNum(int a, int b);
	int multiNum(int a, int b);
	double divNum(double a, double b);

}
