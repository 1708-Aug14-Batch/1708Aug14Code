package com.bank.dao;

public interface DAO {
	
	// The contract we make for the information that
	// we will need to send to and receive from the data source
	
	// createUser
	// readUser
	// updateUser - change any user field except their primary key
	// deleteUser - user is not truly deleted, merely marked as deleted
	// getEmailList - returns a list of every email address in the system
	// getAllUsers

}
